
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the SpendWise application.",
      "properties": {
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        }
      },
      "required": [
        "email"
      ]
    },
    "Expense": {
      "title": "Expense",
      "type": "object",
      "description": "Represents an expense record for a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this expense."
        },
        "title": {
          "type": "string",
          "description": "Title of the expense."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the expense."
        },
        "amount": {
          "type": "number",
          "description": "Amount of the expense."
        },
        "category": {
          "type": "string",
          "description": "Category of the expense."
        },
        "date": {
          "type": "string",
          "description": "Date of the expense.",
          "format": "date-time"
        },
        "timestamp": {
          "type": "number",
          "description": "Timestamp of when the expense was recorded."
        }
      },
      "required": [
        "userId",
        "title",
        "amount",
        "category",
        "date",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Path-based ownership ensures only the authenticated user (identified by userId) can access their own profile data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user. Corresponds to the Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/expenses/{expenseId}",
        "definition": {
          "entityName": "Expense",
          "schema": {
            "$ref": "#/backend/entities/Expense"
          },
          "description": "Stores expense data for each user. Path-based ownership ensures that only the authenticated user can manage their own expenses.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user who owns the expense."
            },
            {
              "name": "expenseId",
              "description": "The unique identifier for the expense document."
            }
          ]
        }
      }
    ]
  }
}
